.. highlight:: shell

============
Installation
============


Stable release
--------------

To install w2v_tissues, run this command in your terminal:

.. code-block:: console

    $ pip install w2v_tissues

This is the preferred method to install w2v_tissues, as it will always install the most recent stable release.

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for w2v_tissues can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/shfong/w2v_tissues

Or download the `tarball`_:

.. code-block:: console

    $ curl  -OL https://github.com/shfong/w2v_tissues/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/shfong/w2v_tissues
.. _tarball: https://github.com/shfong/w2v_tissues/tarball/master
